export enum UserType {
	user = 0,
	moderator = 10,
	administrator = 100,
}

// ----------------------------

export enum UserStatus {
	active = 100,
	banned = -100,
}

// ----------------------------

export enum Lang {
	ru = 'ru',
	en = 'en',
}
