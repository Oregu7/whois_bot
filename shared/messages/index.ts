import * as main from './main-messages';
import * as report from './report-messages';
import * as promocode from './promocode-messages';
import * as services from './services-messages';

export const Messages = {
	main,
	report,
	promocode,
	services,
};
